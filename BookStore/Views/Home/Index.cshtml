@model BookStore.ViewModel.RetriveDataViewModel;
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
<div>
    <table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Title</th>
      <th scope="col">Price</th>
      <th scope="col">Author</th>
      <th scope="col">Procedure</th>

    </tr>
  </thead>
  <tbody>
      @foreach (var book in Model.Books)
    {
    <tr>
        @if(book.Author != null){
      <th scope="row">@book.Id</th>
      <td>@book.Title</td>
      <td>@book.Price</td>
      <td><a asp-action="AuthorDetails" asp-route-id="@book.AuthorId">@book.Author.Name</a></td>
      <td><a asp-action="Edit" asp-route-id="@book.Id">Edit</a> |
                            <span class="link-danger">
                                @Html.ActionLink("Delete", "Delete", new { id = book.Id }, new
                                    {
                                        onclick = "return confirm('Are sure wants to delete ?');"
                                    })
          </span>
       </td>
        }
    </tr>
    }
  </tbody>
</table>
</div>

